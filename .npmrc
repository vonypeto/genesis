# Use hoisted node_modules (centralized)
node-linker=hoisted
shamefully-hoist=true
public-hoist-pattern[]=*

# Auto install peers
auto-install-peers=true

# Strict peer dependencies (set to false for more lenient installations)
strict-peer-dependencies=false

# Enable builds for native modules
enable-pre-post-scripts=true
