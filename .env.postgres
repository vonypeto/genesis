# Local Postgres environment
POSTGRES_USER=app_user
POSTGRES_PASSWORD=app_password
POSTGRES_DB=app_db

# App-side connection string (use in your ConfigService or .env)
# For local dev without SSL:
POSTGRE_URI=postgres://app_user:app_password@localhost:5432/app_db
POSTGRE_POOL_SIZE=10
NODE_ENV=development

# SSL settings (leave unset for local). For production/Aiven, set path to CA file.
# POSTGRE_SSL_CA_PATH=./ca.pem
